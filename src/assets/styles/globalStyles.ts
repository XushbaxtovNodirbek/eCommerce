import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  textStyle: {},
  background: {},
  center: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
